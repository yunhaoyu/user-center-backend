spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://线上数据库地址/yupao
    username: root
    password: 123456
  # session 失效时间
  session:
    timeout: 86400
server:
  address: 0.0.0.0
